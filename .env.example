# Environment variables for local development
# Copy this file to .env and fill in your actual values

# Login credentials (use test accounts only)
LOGIN_USERNAME=pppols
LOGIN_PASSWORD=Password1

# Base URLs for different environments
BASE_URL=https://employer.modoff.secure.azblue.com
LOGIN_URL=https://employer.modoff.secure.azblue.com/log-in

# Test environment settings
NODE_ENV=development
TEST_TIMEOUT=30000
TEST_RETRIES=1
TEST_WORKERS=1

# Optional: Browser settings
HEADLESS=false
SLOWMO=0

# Optional: Video and screenshot settings
CAPTURE_VIDEO=on-failure
CAPTURE_SCREENSHOTS=only-on-failure

# Optional: Test data settings
TEST_ACCOUNT_ID=333536
TEST_USER_DISPLAY_NAME=Patio
